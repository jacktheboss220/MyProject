<div class="container text-center">
    <div class="ytimg">
    </div>
    <video controls>
        <source src="../{{video}}" type="video/mp4">
        Your browser does not support HTML video.
    </video>
    <div class="fs-6 font-monospace text-center text-white">{{title}}</div>
    <div class="button">
        <a class="me-1 btn btn-primary" href="../{{{video}}}" target="_blank">
            Video
        </a>
        {{!-- <a class="ms-1 btn btn-primary" href="{{{audio}}}" target="_blank">
            Audio
        </a> --}}
    </div>
</div>